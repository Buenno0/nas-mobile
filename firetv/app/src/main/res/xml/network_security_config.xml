<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
  <!-- URLs HTTP ainda passam pela validação estrita de host local no app. -->
  <base-config cleartextTrafficPermitted="true" />
</network-security-config>
